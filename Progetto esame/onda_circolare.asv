clear; clc; close all; 
k = 5;
x0 = 1 + 1i;


u_circ = @(x) exp(1i * k * abs(x - x0));

x_plot = linspace(real(x0)-5, real(x0)+5, 400);
[x,y] = meshgrid(linspace(-5,5,400));
z = x + 1i*y;

u = u_circ(z);

pcolor(x, y, imag(u)); shading flat
colorbar
title('Onda circolare (senza decadimento)')